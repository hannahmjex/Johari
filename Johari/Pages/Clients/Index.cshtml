@page
@model Johari.Pages.Clients.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Responses</h1>

<form method="post">
    <div class="boarder backgroundWhite container">
        <table>
            <tr><th>Checkboxes</th><th>Adjectives</th></tr>
            <tr>

                @{
                    for (int i = 0; i < Model.Adjectives.Count(); i++)
                    {
                        <td>
                            <input asp-for="@Model.Adjectives[i].Value" type="hidden" />
                            <input asp-for="@Model.Adjectives[i].Text" type="hidden" />
                            <input asp-for="@Model.Adjectives[i].Selected" />
                        </td>
                        <td>
                           @Model.Adjectives[i].Text
                        </td>
                        //
                        //       string description = Model.Adjectives[i].Text.ToString();
                        //       string[] descriptions = description.Split(' ');
                        //       for (int j = 0; i < descriptions.Length; j++)
                        //       {
                        //           <td>
                        //               descriptions[j];
                        //           </td>
                        //       }

                    }

                }

            </tr>
        </table>
    </div>
</form>

